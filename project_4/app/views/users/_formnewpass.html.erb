<%# Displays any alert messages if issues with creating user. %>
<% flash.each do |type, msg| %>
  <div>
    <%= msg %>
  </div>
<% end %>

<%= form_tag newpassword_path do |form|  %>
  <%# Format to create a new user. %>

  <div class= "container">
  <div class="field">
    <%= label_tag "Old Password"%>
    <%= password_field_tag :password %>
  </div>
  <div class="field">
    <%= label_tag "NewPassword" %>
    <%= password_field_tag :newpassword %>
  </div>
</div>
  <div class="button-bucket">
  <button type="submit" class="button1"> Submit Password </button>
  <%= link_to "Back", root_path, :class => "button button2" %>
</div>
<% end %>
